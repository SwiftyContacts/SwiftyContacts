# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage
language: objective-c
osx_image: xcode8.3
xcode_sdk: iphonesimulator10.0
script:
- set -o pipefail
# cache: cocoapods
# podfile: Example/Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install --project-directory=Example
script:
- xcodebuild test -enableCodeCoverage YES -workspace Example/SwiftyContacts.xcworkspace -scheme SwiftyContacts-Example -sdk iphonesimulator10.0 ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint

after_success:
- bash <(curl -s https://codecov.io/bash)

 notifications:
    slack: satishbabariya:9DPFvV4rg7dO755LhoNsCfGV
